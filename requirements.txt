pydantic>=2.0.0
openai>=0.28.1,<1.0.0
python-dotenv==1.0.0
pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'  # For macOS menu bar support
tkinter; platform_system != 'Darwin'  # For non-macOS systems
